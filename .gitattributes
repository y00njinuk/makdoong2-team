# 진입점(bin/cli.ts · postinstall.mts · scripts/*.mts)은 컴파일하지 않고 node
# 네이티브 type-stripping 으로 소스를 직접 실행한다. 저장소에는 소스만 존재한다.
# (dist/ 는 플러그인 런타임 빌드로 .gitignore 대상이라 저장소에 없다.)
*.sh    text eol=lf
*.ts    text eol=lf
*.mts   text eol=lf
