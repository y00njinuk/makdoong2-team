{
  "//": "배포 tarball 용 진입점 빌드. 산출물(.js/.mjs)은 .gitignore 대상 — repo 에 커밋하지 않고 prepack/prepare 가 tarball·설치용으로만 만든다.",
  "//2": "왜 필요한가: node 는 node_modules 안의 .ts 를 type-stripping 하지 않는다(ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING). 설치된 패키지의 bin/postinstall 은 실행 가능한 .js 여야 한다. (개발 중 repo 에서는 소스를 직접 실행하므로 이 빌드가 필요 없다.)",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "rootDir": ".",
    "outDir": ".",
    "declaration": false,
    "sourceMap": false,
    "removeComments": false,
    "newLine": "lf",
    "rewriteRelativeImportExtensions": true,
    "allowImportingTsExtensions": true,
    "resolveJsonModule": false,
    "noEmitOnError": true
  },
  "//files": "런타임에 실행되는 진입점만. 테스트 러너(run-tests/smoke-test/test-postinstall)는 repo 에서 소스로 실행되므로 빌드 대상이 아니다.",
  "files": [
    "bin/cli.ts",
    "postinstall.mts",
    "scripts/install-lib.mts",
    "scripts/model-policy.mts"
  ]
}
